{
    "compilerOptions": {
        "target": "ES6",
        "baseUrl": "./",  // all paths are relative to the baseUrl
        "paths": {
            // "~/*" : ["*"],   // resolve any `~/foo/bar` to `<baseUrl>/foo/bar`
            "~/pages/*" : ["pages/*"], 
            "~/views/*" : ["views/*"]  
        }
    },
    "exclude": [
        "node_modules",
        "**/node_modules/*"
    ]
}
